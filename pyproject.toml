[project]
name = "addressbook-streamlit"
description = "Sample address book app with Streamlit, SQLite and a pywebview desktop launcher"
version = "0.1.0"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "mcp>=2.2.0",
    "pywebview>=6.2.1",
    "streamlit>=1.64.0",
    "uvicorn>=0.53.0",
]

[dependency-groups]
dev = [
    "httpx2>=2.13.1",
    "pytest>=9.1.1",
]
